<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{title}}</p>
  <p>{{description}}</p>
  <p>{{ingredients}}</p>
   <p>{{instruction}}</p
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="knife">🔪</span>
  </p>
  <p>
    <button class="btn btn-primary pulse">Save to Favorites</button>
  </p>
</div>